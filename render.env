# Render.com Environment Variables Configuration
# Copy these values to your Render dashboard: Environment → Environment Variables
# 
# IMPORTANT: Replace placeholder values with your actual values before deploying!

# ============================================
# REQUIRED - Flask Configuration
# ============================================
FLASK_APP=backend/app.py
FLASK_ENV=production
FLASK_DEBUG=0

# SECRET_KEY - Generate a strong random key (REQUIRED)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
# Or use: openssl rand -hex 32
# ⚠️ IMPORTANT: This is a generated key - use this exact value in Render
SECRET_KEY=3f794a885c4d93a27e7d070f3d8be989f21c27055476ce0ca816b817a55e3cc3

# ============================================
# Server Configuration (Render sets PORT automatically)
# ============================================
# Render automatically sets PORT, but you can override if needed
# PORT=10000

# Host should be 0.0.0.0 (Render sets this automatically)
# HOST=0.0.0.0

# ============================================
# Database Configuration
# ============================================
# SQLite (default - filesystem storage, data persists during service lifetime)
# Note: On Render, SQLite files are stored in the service's filesystem
# For production, consider using Render PostgreSQL database
DATABASE_URL=sqlite:///backend/health_system.db

# If using Render PostgreSQL, uncomment and set:
# DATABASE_URL=postgresql://user:password@hostname:5432/dbname

# ============================================
# Firebase Configuration (REQUIRED for OTP)
# ============================================
# Get your Firebase Web API Key from Firebase Console
# Project Settings → General → Web API Key
FIREBASE_WEB_API_KEY=your-firebase-web-api-key-here

# Firebase Service Account (Optional - for full Firebase features)
# If you have a service account JSON file, you'll need to:
# 1. Convert JSON to environment variable (see RENDER_DEPLOYMENT.md)
# 2. Or use Render's Secret Files feature
# FIREBASE_CREDENTIALS_PATH=backend/pkl/firebase_service_account.json

# ============================================
# OTP Configuration
# ============================================
OTP_CODE_LENGTH=6
OTP_EXPIRY_MINUTES=10

# ============================================
# File Upload Configuration
# ============================================
# Maximum upload size in bytes (16MB = 16777216)
MAX_UPLOAD_SIZE=16777216
UPLOAD_FOLDER=backend/uploads

# ============================================
# Production Settings
# ============================================
PRODUCTION=True
LOG_LEVEL=INFO

# ============================================
# Render-Specific Settings
# ============================================
# Render automatically provides:
# - PORT (automatically set by Render)
# - RENDER environment (set to "true" on Render)
# - RENDER_SERVICE_NAME (name of your service)
# - RENDER_SERVICE_ID (unique service ID)

# Optional: Enable if you want to detect Render environment
# RENDER=true

